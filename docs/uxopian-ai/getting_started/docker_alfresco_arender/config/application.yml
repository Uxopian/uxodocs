app:
  base-url: ${APP_BASE_URL:}

server:
  servlet:
    context-path: ${CONTEXT_PATH:}
  port: ${UXOPIAN_AI_PORT:8080}

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:}
  web:
    resources:
      static-locations: "classpath:/public/,classpath:/static/,file:/app/webcomponents/"
