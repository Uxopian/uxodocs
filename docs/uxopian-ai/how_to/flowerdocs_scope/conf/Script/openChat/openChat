(function () {
  createAIAction({
    label: {
      name: "openChat",
      FR: "Ouvrir une discussion",
      EN: "Open a chat",
    },

    onExecute: () => {
      return openChatComp({
        inputs: [contextualInput()],
      });
    },
  });
})();
